const g=globalThis
if(!g.__azarragaStore) g.__azarragaStore={documents:[],quotes:[],jobs:[],invoices:[]}
export const store=g.__azarragaStore
export function id(prefix){return `${prefix}-${Date.now().toString(36).toUpperCase()}-${Math.random().toString(36).slice(2,6).toUpperCase()}`}
export function normalizeRecord(input={}){return {id:input.id||id('DOC'),kind:input.kind||'unknown',customer:String(input.customer||''),project:String(input.project||''),location:String(input.location||''),documentNumber:String(input.documentNumber||''),date:String(input.date||''),status:input.status||'needs_review',items:Array.isArray(input.items)?input.items.map(x=>({description:String(x.description||''),product:String(x.product||''),qty:Number(x.qty||0),unit:String(x.unit||'set'),widthMm:Number(x.widthMm||0),heightMm:Number(x.heightMm||0),glass:String(x.glass||''),frame:String(x.frame||''),unitPrice:Number(x.unitPrice||0),amount:Number(x.amount||0)})):[],logistics:Number(input.logistics||0),discount:Number(input.discount||0),total:Number(input.total||0),terms:String(input.terms||''),source:input.source||'manual',createdAt:input.createdAt||new Date().toISOString()}}
export function validateRecord(r){const warnings=[];if(!r.customer)warnings.push('Customer missing');if(!r.project)warnings.push('Project missing');if(!r.items.length)warnings.push('No line items');if(r.items.some(x=>x.qty<0||x.unitPrice<0||x.amount<0))warnings.push('Negative line value');return warnings}
